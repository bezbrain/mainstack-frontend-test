import { Balances, Graph } from ".";

const Hero = () => {
  return (
    <section className="pt-32 flex justify-between">
      <Graph />
      <Balances />
    </section>
  );
};

export default Hero;
