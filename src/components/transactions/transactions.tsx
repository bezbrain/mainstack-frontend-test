import { SectionWrapper } from "../general/wrappers";
import { TransHeader } from ".";

const Transactions = () => {
  return (
    <SectionWrapper className="mt-16">
      <TransHeader />
    </SectionWrapper>
  );
};

export default Transactions;
